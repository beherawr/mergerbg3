# Development dependencies. End users don't need any of these: the
# released .exe is self-contained. Only needed if you're running the
# test suite or building the exe yourself.

# Runtime deps
PySide6>=6.6
lxml>=4.9
# Pillow: used by the Add-Icon feature to convert PNG to BC3/DXT5 DDS at
# the sizes BG3 needs. 12.0+ has the DDS DXT5 encoder we rely on.
Pillow>=12.0

# Build
pyinstaller>=6.0

# Test
pytest>=7.0
